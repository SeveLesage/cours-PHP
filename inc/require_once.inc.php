<h2>require_once()</h2>

<p>Grâce à la fonction <code>require_once()</code> , on a les même actions qui <code>require()</code> sauf que l'on ne pourra appeler un fichier qu'une seule fois dans la page</p>